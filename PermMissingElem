def solution(A):
    
  

  test_array= [x for x in range(1, len(A)+1)]

  s = dict()

  for i in range(len(A)): 
      s[A[i]] = 1


  for i in range(len(test_array)): 
      if test_array[i] not in s.keys():

          return test_array[i]
